<!-- <view class="interest">
  <view class="head"></view>
  <view class="high">
    <p>\r\t高级</p>
      <checkbox-group bindchange="checkboxChange">
        <view class="checkbox-frame" wx:for="{{items1}}" style="background-color:#E8FFF5;">
          <checkbox class="checkbox" value="{{item.name1}}" checked="{{item.checked1}}"/>{{item.value}}
        </view>
      </checkbox-group>
  </view>
  <view class="middle">
    <text>\r\t中级</text>
      <checkbox-group bindchange="checkboxChange">
        <view class="checkbox-frame" wx:for="{{items2}}" style="background-color:#FFFFDF;">
          <checkbox class="checkbox" value="{{item.name2}}" checked="{{item.checked2}}"/>{{item.value}}
        </view>
      </checkbox-group>
  </view>
  <view class="low">
    <text>\r\t初级</text>
      <checkbox-group bindchange="checkboxChange">
        <view class="checkbox-frame" wx:for="{{items3}}" style="background-color:#ECECFF;">
          <checkbox class="checkbox" value="{{item.name3}}" checked="{{item.checked3}}"/>{{item.value}}
        </view>
      </checkbox-group>
  </view>
  
</view> -->

<!-- <view class="cu-custom" style="height:{{CustomBar}}px;">
   <view class="cu-bar fixed none-bg text-white bg-img" style="height:{{CustomBar}}px;padding-top:{{StatusBar}}px;">
    <view class='content' style='top:{{StatusBar}}px;'>
      <image src="/images/cjkz.png" mode='widthFix'></image>
    </view> 
  </view>
</view> --> 
 <view class="cu-card">
  <view class="cu-item bg-img shadow-blur margin-left"  bindtap="toChild" wx:for="{{list}}" wx:key       style="background-image:url({{item.img}})" class="nav-li bg-{{item.color}}">
    <view class="cardTitle">
      {{item.title}}
    </view>
  </view>
</view>
<view class='cu-tabbar-height'></view>